livekit-agents[openai,silero,turn-detector]~=1.6
python-dotenv~=1.0
aiohttp~=3.9

# Self-hosted only: LiveKit Inference does not serve a self-hosted server, so
# each vendor is called directly. Harmless on LiveKit Cloud — never imported.
livekit-plugins-sarvam~=1.6
livekit-plugins-deepgram~=1.6
livekit-plugins-cartesia~=1.6
livekit-plugins-elevenlabs~=1.6
# The one whose free tier clones voices — see docs/ai-screening-calls.md.
livekit-plugins-inworld~=1.6
